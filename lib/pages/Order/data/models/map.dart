import 'package:google_maps_flutter/google_maps_flutter.dart';

class InitMap {
  CameraPosition cameraPosition;
  String map_style;
  InitMap({required this.cameraPosition, required this.map_style});
}
